#include <iostream> //добавление библиотеки ввода/ввывода
#include <math.h> //добавление библиотеки математических функций

using namespace std;
//Точка входа в программу - главная функция
int main()
{
    setlocale(LC_ALL, "Russian"); // русификация 
    // объявление переменных
    int x1, x2, y1, y2, x3, y3, a, b, c, result_p, result_s;
    //ввод переменных с клавиатуры
    cout << "Введите x1: ";
    cin >> x1;
    cout << "Введите y1: ";
    cin >> y1;
    cout << "Введите x2: ";
    cin >> x2;
    cout << "Введите y2: ";
    cin >> y2;
    cout << "Введите x3: ";
    cin >> x3;
    cout << "Введите y3: ";
    cin >> y3;
    // вычисления
    a = sqrt(((x2 - x1) * (x2 - x1)) + ((y2 - y1) * (y2 - y1)));
    b = sqrt(((x2 - x3) * (x2 - x3)) + ((y2 - y3) * (y2 - y3)));
    c = sqrt(((x3 - x1) * (x3 - x1)) + ((y3 - y1) * (y3 - y1)));
    result_p = (a + b + c) / 2;
    result_s = sqrt(result_p * (result_p - a) * (result_p - b) * (result_p - c));
    cout << "Периметр: " << result_p << endl << "Площадь: " << result_s; // вывод результата на экран
}
